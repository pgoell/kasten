# Directory Update Log

## 2026-08-05

* **Creation**: Started this bundle. Structure follows Diátaxis, file format follows OKF v0.2.
* **Removal**: Deleted 14 documents describing Sieve, a newsletter digest platform that shares no code with kasten. They were `docs/vision.md`, five ADRs under `docs/adr/`, five architecture pages under `docs/architecture/`, and two files under `docs/superpowers/`.
* **Creation**: Added [Getting started](/tutorials/getting-started.md).
* **Creation**: Added [Add a database migration](/how-to/add-a-database-migration.md), [Regenerate the API types](/how-to/regenerate-the-api-types.md) and [Run the checks](/how-to/run-the-checks.md).
* **Creation**: Added [mise tasks](/reference/mise-tasks.md), [HTTP API](/reference/http-api.md) and [Configuration](/reference/configuration.md).
* **Creation**: Added [The vault and the derived index](/explanation/vault-and-derived-index.md) and [Two environments](/explanation/environments.md).
* **Update**: [HTTP API](/reference/http-api.md) gained `GET /api/files/{path}`, which reads one note, and lost the line saying note content is not exposed.
* **Update**: [HTTP API](/reference/http-api.md) gained `PUT /api/files/{path}`, which writes one note back to the vault, and lost the line saying nothing writes yet.
* **Update**: [Getting started](/tutorials/getting-started.md) gained a step that opens a note and edits it. Its "what you cannot do yet" section no longer claims that clicking a note does nothing and that saving is missing.
* **Update**: [Getting started](/tutorials/getting-started.md) describes the save state as the ring in the status bar rather than the words "Saved" and "Unsaved changes", which the bar no longer carries.
* **Creation**: Added [Recover an earlier version of a note](/how-to/recover-an-earlier-version.md), now that a save is recorded in the vault's jj repo.
* **Update**: [HTTP API](/reference/http-api.md) says what a write records in jj, and that a vault without a repo is written to just the same.
* **Update**: [Getting started](/tutorials/getting-started.md) gained an optional step that gives the vault a history.
* **Update**: [The vault and the derived index](/explanation/vault-and-derived-index.md) gained the consequence that a note's history lives with the note and not in a table.
* **Creation**: Added [Live preview and the vim mode](/explanation/live-preview.md), now that the editor renders markdown rather than colouring it.
* **Update**: [Getting started](/tutorials/getting-started.md) says the note is rendered, and that `i` shows the markdown behind the line as well as starting insert mode.
* **Update**: [Getting started](/tutorials/getting-started.md) says tables, images and code fences keep their syntax, which live preview does not render.
* **Update**: [Live preview and the vim mode](/explanation/live-preview.md) says why the mode effect is dispatched a microtask late, after a synchronous dispatch was found to kill the vim plugin on leaving visual mode.
* **Creation**: Added [Editor keys](/reference/editor-keys.md), now that space is the leader key and the editor carries formatting commands and a navigable file tree.
* **Update**: [Getting started](/tutorials/getting-started.md) folds the tree with space then `b` rather than Ctrl+B, which now means bold, and points at the new keys page.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>e`, which moves the focus to the file tree, and a section on Tab and Shift+Tab. The file tree section no longer says Tab reaches the tree, because Tab now indents.
* **Update**: [Live preview and the vim mode](/explanation/live-preview.md) says why a drawn bullet leaves with the dash it stands in for while a drawn blockquote bar stays, and where a nested item's indent comes from once its spaces are hidden.
* **Update**: [Getting started](/tutorials/getting-started.md) says the editor holds the cursor from the moment the page loads, and reaches the tree with space then `e` rather than by clicking it.
* **Update**: [Getting started](/tutorials/getting-started.md) says the foot of the window carries the weekday, date, calendar week and time beside the save ring.
* **Update**: [Live preview and the vim mode](/explanation/live-preview.md) counts a fenced code block among what it renders rather than what it does not, now that every line of one takes a line decoration and needs no widget.
* **Update**: [Editor keys](/reference/editor-keys.md) gained a section on backticks, which close themselves singly and open a whole fence in threes.
* **Update**: [Getting started](/tutorials/getting-started.md) no longer lists fenced code blocks among what live preview leaves as syntax.
* **Update**: [Live preview and the vim mode](/explanation/live-preview.md) says a horizontal rule is painted across the row with its dashes hidden, and that a setext underline is left alone because the parser already tells the two apart.
* **Update**: [HTTP API](/reference/http-api.md) gained `POST /api/files/{path}`, which starts an empty note and makes the folders on the way to it, and says why this one names its refusal while the read and the write answer `404` to everything. The `PUT` section no longer says there is no endpoint for creating notes.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>cf` and a section on the new note prompt it opens. It is the first binding on the page to take two keys after the leader.
* **Update**: [Getting started](/tutorials/getting-started.md) gained a step that makes a note from the browser, and its description now promises notes of your own rather than a list of them. Its "what you cannot do yet" section no longer says a new note is a file you make yourself.
* **Creation**: Added [Note prompt performance](/reference/ranking-performance.md). The bar is 16ms for a keystroke at 10,000 notes, about 4ms of it JS, and the prompt was costing 16.8ms. Capping the list at twenty rows and deriving the folder set once per vault rather than once per keystroke brought it to 4.9ms. The page states the bar, the three vitest projects that measure it, why the benchmark records rather than gates, and every recorded number, the ungated load side included.
* **Update**: [mise tasks](/reference/mise-tasks.md) gained `fe:frame`, which runs the Chromium gates, and `fe:bench`, which records numbers and gates nothing. `fe:test` now says it runs the `unit` and `perf` projects, because the third boots a browser CI's Test job does not install.
* **Update**: [HTTP API](/reference/http-api.md) gained `PATCH /api/files/{path}`, which gives a note a new path, moving it between folders as well as renaming it, and takes the folders it emptied with it. The section says why the path is the note's identity rather than a verb in the URL, and why the reply reads the text off disk instead of carrying the client's copy across.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>rf`. "The new note prompt" is now "The note prompt", because one prompt does both jobs, and the section says which line the two modes read differently: a path a note is already at opens it on a create and is refused on a rename.
* **Update**: [Getting started](/tutorials/getting-started.md) gained a step that renames a note and moves it into a folder that does not exist yet, and one that watches a rename bounce off a name already taken. Its "what you cannot do yet" section no longer says a note cannot be renamed, and now says folders still are renamed by hand.

## 2026-08-06

* **Update**: [HTTP API](/reference/http-api.md) gained `PATCH /api/folders/{path}`, which gives a folder a new path and takes every note under it along. The section says why a folder needs a route of its own rather than the note's, why the move is one rename rather than a walk over the subtree, and why merging into a folder that already exists is refused.
* **Update**: [Editor keys](/reference/editor-keys.md) gained the tree's `c` and `r`, the first bare letters on the page that are not navigation. `<leader>rf` now says the `f` names a file and that `r` is the key for a row of either kind. "The note prompt" covers the third mode, `rename folder`, which adds no `.md`, leaves the folder's own subtree out of the completion list, and says how many notes the move carries.
* **Update**: [Getting started](/tutorials/getting-started.md) gained a step that moves a whole folder and watches the vault refuse a folder inside itself and a folder onto one that exists. Its "what you cannot do yet" section no longer says folders are renamed by hand, and says instead that a folder is never made on its own.
* **Creation**: Added [The note finder](/reference/editor-keys.md#the-note-finder) to Editor keys, reached by `<leader>ff` and the tree's `f`. The section says what the list ranks, what the pane beside it shows, and why Tab is unbound there: the finder is the other way round from the prompt, the list being the answer and the input only a filter.
* **Rename**: [Note prompt performance](/reference/ranking-performance.md) became [Ranking performance](/reference/ranking-performance.md), one scorer now serving the prompt and the finder both. Every recorded number was measured again, and every threshold with them: ranking the folders a keystroke pays for fell from 0.372ms to 0.092ms, and the prompt's keystroke in Chromium from 5.1ms to 2.5ms. The page gained the finder's own numbers, 6.7ms for a keystroke at 10,000 notes and about 21ms at 50,000, and says why ranking notes with the old scorer would have cost 156ms.
* **Update**: [HTTP API](/reference/http-api.md) gained `GET /api/search`, which finds every line in the vault holding a query. The section says why nothing is indexed, why the match is literal rather than fuzzy, which flags keep it seeing exactly what `GET /api/files` lists, and why a blank query answers with nothing rather than everything.
* **Creation**: Added [The note search](/reference/editor-keys.md#the-note-search) to Editor keys, reached by `<leader>fg` and the tree's `s`. The section says why the work splits between a literal match on the server and a ranking in the browser, why it cannot be the other way round, and why the tree's key is `s` when the leader's is `g`.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>fg` and the tree's `s`. The file tree section now covers four bare letters rather than three, and says why `g` could not be one of them.
* **Update**: [Getting started](/tutorials/getting-started.md) no longer lists search among what is not built, and names the key that opens it.
* **Update**: [The note search](/reference/editor-keys.md#the-note-search) gained the preview pane, which shows the note around the highlighted hit with the matching line marked and centred. It is a window of thirty lines either way rather than the whole note, so a long note costs what a short one does, and two hits in one note re-centre it without a second read.
* **Update**: [The note finder](/reference/editor-keys.md#the-note-finder) and [The note search](/reference/editor-keys.md#the-note-search) both render the note in their preview pane now, using the editor's own live preview rather than a second, plainer one. The finder's page no longer says its pane is plain text. The search section says the window can slice a fenced code block, which rendering makes visible where plain text did not.
* **Update**: [The note search](/reference/editor-keys.md#the-note-search) is drawn at the finder's size rather than wider than it. Both panels, and the note prompt beside them, now take their backdrop, box, header, input, rows and status line from one table of classes, so the three cannot drift apart again.
* **Creation**: Added [Wikilinks](/reference/editor-keys.md#wikilinks) to Editor keys, now that `[[note]]` is parsed, rendered as the name alone, and followed with `gf`. The section says how a target is resolved, a path taken at its word and a bare name looked for anywhere in the vault, why following a link to a note that is not there makes it, and why both edges of a link count as on it.
* **Update**: [Getting started](/tutorials/getting-started.md) no longer lists wikilinks among what is not built, and says backlinks are the half that is still missing.
* **Update**: [Wikilinks](/reference/editor-keys.md#wikilinks) gained ctrl+click, which follows a link with the mouse where a plain click still puts the cursor in it, and says why the click reads the element under the pointer rather than the nearest position. A link to a note that is not there is drawn muted and dotted now, and the section says why that is an invitation rather than a warning, and why a view with no vault listing marks nothing dead.
* **Creation**: Added [Completing a link](/reference/editor-keys.md#completing-a-link) to Editor keys. Typing `[[` offers the vault's notes, and taking one closes the link, because markdown's close-brackets answers a `[` with nothing.
* **Update**: [HTTP API](/reference/http-api.md) says what `PATCH /api/files/{path}` and `PATCH /api/folders/{path}` do to the links that named what they moved. Each one is rewritten in the spelling it had, so a bare name is left alone by a move between folders and follows a change of name. The sections say why the rewrite runs before the move and inside the same jj change, why the resolution rule is written out on both sides, and why a folder is matched as a whole path segment.
* **Update**: [HTTP API](/reference/http-api.md) says rg picks the notes a move reads, and why the query it is given can miss none of them. The numbers are on the page: 158ms for a move at 10,000 notes against 315ms for the walk over every note it replaced, 134ms of what remains being the directory listing that `GET /api/files` already pays. It also says why an rg that cannot read the whole vault fails the move here rather than answering with nothing, which is what search does.
* **Creation**: Added [The link panels](/reference/editor-keys.md#the-link-panels) to Editor keys, reached by `<leader>gb` and `<leader>go`. The section says why each reuses a panel that was already here, backlinks being lines and outgoing links being notes, how a backlink is told from a note's name in prose, and why a link to a note nobody has written yet is left out.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>gb` and `<leader>go`, and says a rename moves the links along with the note. Tab now walks the list in the finder and the search rather than being unbound, so both key tables carry it, and the finder's section says why the key is answered even with nothing to walk.
* **Update**: [The load side](/reference/ranking-performance.md) records `GET /api/files` at 15.9ms rather than 155 to 161ms, `list_markdown_files` now walking the vault with `os.scandir` instead of `rglob`, and no longer says nothing on that path was changed. The section says what `rglob` was spending the time on, a `Path` per entry and the relative path parsed back out of it three times, and that the two browser figures under it were recorded before the change and still carry the old request.
* **Update**: [HTTP API](/reference/http-api.md) gives what `GET /api/files` costs, says a hidden directory is skipped without being walked into, and says a directory is never listed whatever its name ends in: reading one is a 404, so a folder called `notes.md` used to be a row in the tree that answered nothing.
* **Update**: [The load side](/reference/ranking-performance.md) records the tree folded rather than unfolded: 8 rows mounted at 10,000 notes rather than 10,842, 10 to 34ms to the first row rather than 443 to 462, and 15 to 65ms to the paint rather than 834 to 859. The section says the file tree held the set of folders that were folded away and that set began empty, which is why nothing collapsed meant everything expanded, and that it holds the set that is unfolded now. It also says the browser rows make no request, which the page used to leave a reader to guess.
* **Update**: [The file tree](/reference/editor-keys.md#the-file-tree) says the tree opens with every folder folded away, that a folder's contents are not drawn until you open it, and that the folders on the way to the open note are unfolded for you so a reload lands on the note rather than on a tree that has hidden it.
* **Creation**: Added [Note frontmatter](/reference/note-frontmatter.md), now that every note carries a YAML block. The page says which three fields kasten manages, why the id is a UUID version 7 and the dates are UTC, why the block is read as lines rather than parsed as YAML, and that a query naming a field matches the block in every note.
* **Update**: [HTTP API](/reference/http-api.md) says a `PUT` stamps the text on the way through and answers with what landed on disk, and that a `POST` starts a note holding its block rather than an empty file. `PUT` no longer says the text is written unchanged.
* **Creation**: Added [Panes and tabs](/reference/editor-keys.md#panes-and-tabs) to Editor keys, now that the window divides the way tmux divides a terminal. The section says why the split keys are `%` and `"`, why splitting the same way twice gives even thirds rather than a half and two quarters, why panes are walked in order rather than by direction, and how `<leader>q` walks back out one press at a time: the note, then the pane, then the tab.
* **Update**: [Editor keys](/reference/editor-keys.md) gained `<leader>ct`, `<leader>o`, `<leader>th`, `<leader>tl`, `<leader>%`, `<leader>"` and the ten digits that go straight to a tab. `<leader>q` no longer says it closes the note and stops there. The page says the arrangement is not in the URL: `?note=` names the note in the focused pane and a reload comes back to it in a single pane.
